package edu.uth.manga.enums;

public enum ChapterStatus {
    DRAFT,       // Bản nháp, chỉ tác giả thấy
    PENDING,     // Đang chờ Admin duyệt
    PUBLISHED,   // Đã duyệt, hiển thị cho người đọc
    REJECTED     // Bị từ chối duyệt
}